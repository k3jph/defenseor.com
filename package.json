{
  "name": "defenseor-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-check.mjs"
  },
  "dependencies": { "astro": "7.3.4" },
  "devDependencies": { "playwright": "1.58.2" }
}
